CREATE INDEX IN_dept_DEPTNO ON dept (DEPTNO)
ALTER TABLE dept ADD INDEX IN_DEPT_DNAME (DNAME)
SELECT 12/3
ALTER TABLE emp ADD SDA INT(20)  DEFAULT 0


select empno,ENAME,sal 
	from emp
	WHERE SAL= ANY(	SELECT MIN(SAL) 
			FROM EMP 
			GROUP BY DEPTNO
		      )
	GROUP BY DEPTNO


SELECT DNAME,COUNT(EMPNO)
	FROM EMP,DEPT
	WHERE EMP.DEPTNO=DEPT.DEPTNO
	GROUP BY DEPT.DEPTNO


SELECT DNAME,ROUND(MIN(SAL)),ROUND(MAX(SAL))
	FROM EMP E,DEPT D
	WHERE E.DEPTNO=D.DEPTNO
	GROUP BY E.DEPTNO
	HAVING COUNT(D.DEPTNO)>2
	