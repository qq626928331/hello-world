mysql高级查询
   1.分组函数
       五种常用的分组函数：
        1.1 MIN 最小值
        1.2 MAX 最大值
        1.3 SUM 求和
        1.4 AVG 平均值    保留小数点后四位，小数点之前取整（默认为int类型）
        1.5 COUNT 计数
        0与null的区别  null 不参与分组函数的运算  0是参与的
   2. ifnull 函数 
     AVG(IFNULL(comm,0)) 如果 comm 是null 强制吧null值 当做0 来计算
   3. group by 字句与where 子句中的 组函数 限制条件不同同时使用
      可以使用having字句 来 添加组函数的 限制条件 
      注意：having 字句应该写在 group by 这句后面


   4. group by > having > order by
      
      查询 每个岗位 工资总和大于5000且岗位名称不是以sales开头的岗位名称和工资总和 并且以工资总和升序排序

  1  SELECT    job, SUM(sal) PAYROLL
  2  FROM      emp
  3  WHERE	  job NOT LIKE 'SALES%'
  4  GROUP BY  job
  5  HAVING    SUM(sal)>5000
  6  ORDER BY  SUM(sal);
  

写出sql语句编译时读取顺序
 备选答案A：213456
 备选答案B：234516
 备选答案C：234561 
 备选答案D：214356
答案：B
 
重要：很重要
SELECT语句执行过程：
1.通过FROM子句中找到需要查询的表；
2.通过WHERE子句进行非分组函数筛选判断；
3.通过GROUP BY子句完成分组操作；
4.通过HAVING子句完成组函数筛选判断；
5.通过SELECT子句选择显示的列或表达式及组函数；
6.通过ORDER BY子句进行排序操作。

子查询：
  分别 可以嵌套在 from 字句 where 字句 和 having字句中


多行子查询
   连接运算符 in Any ALL 
   any的使用：
     < ANY：表示小于子查询结果集中的任意一个，即小于最大值就可以

     > ANY：表示大于子查询结果集中的任意一个，即大于最小值就可以
   
     = ANY : 与 in 效果一样

    ALL的使用：
     < ALL:表示小于子查询结果集中的所有行，即小于最小值。

     > ALL:表示大于子查询结果集中的所有行，即大于最大值。
    
     =ALL 无意义

    子查询中 对于空值的操作
         如果子查询中有空值,返回的结果集在运算符连接过程中,结果集也是为空的,所以子查询中的数据一定非空的。









      
